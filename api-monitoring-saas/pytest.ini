[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --no-header
    -p no:warnings
    --timeout=60
log_cli = true
log_cli_level = WARNING
